let num = 3;

// num = "3";

num = 4;

let str: string = "hello";

// str = 3;

let bool = true;

function add(lhs: number, rhs: number): number {
  return lhs + rhs;
}

const notWeirdResult: number = add(3, 4);
